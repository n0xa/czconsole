// Allow the deprivileged czconsole web worker (_czconsole) to start/stop the
// SDR capture units. The worker writes validated parameters to an env file in
// /run/czconsole/ then starts the unit; the unit (as the operator user) reads
// the env file and runs rtl_power or rtl_433. No setuid is involved.
//
// Install to /etc/polkit-1/rules.d/52-czconsole-sdr.rules
polkit.addRule(function(action, subject) {
    if (action.id == "org.freedesktop.systemd1.manage-units" &&
        subject.user == "_czconsole") {
        var unit = action.lookup("unit");
        if (unit == "czconsole-rtlpower.service" ||
            unit == "czconsole-rtl433.service") {
            return polkit.Result.YES;
        }
    }
});
